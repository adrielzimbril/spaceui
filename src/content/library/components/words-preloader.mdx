---
title: Words Preloader
description: Greeting cycle preloader that exits with a curved SVG wipe. Replay from the demo.
preview: demo-c-words-preloader-01
---

The overlay is `absolute` inside a relative parent so it does not cover the docs chrome. Source: `registry/components/spaceui/words-preloader/index.tsx`. Remount with a `key` to replay.

## Installation

<ComponentInstallation name="components-spaceui-words-preloader" />

## Usage

```tsx
import { WordsPreloader } from '@/components/spaceui/words-preloader'

export default function SpaceUI() {
  return (
    <WordsPreloader words={['Hello', 'bonjour', 'Ciao']} duration={2000}>
      <YourPage />
    </WordsPreloader>
  )
}
```

## Files

| File                        | Role                               |
| --------------------------- | ---------------------------------- |
| `words-preloader/index.tsx` | Overlay, word index, SVG path wipe |

## API Reference

| Prop        | Type        | Default       | Description                          |
| ----------- | ----------- | ------------- | ------------------------------------ |
| `words`     | `string[]`  | greetings     | Sequence shown before the wipe       |
| `duration`  | `number`    | `2000`        | Milliseconds before exit             |
| `className` | `string`    | —             | Root class                           |
| `children`  | `ReactNode` | fallback copy | Content revealed after the preloader |
