---
title: React Hooks & Utilities
description: Complete collection of 80 production-ready React hooks, declarative control-flow components, and pure utilities by Space UI.
---

## Complete Category Summary

| Category               | Total Items      | Description                              | Key Exports                                                                                                          |
| :--------------------- | :--------------- | :--------------------------------------- | :------------------------------------------------------------------------------------------------------------------- |
| **🌐 Browser Hooks**   | **18 hooks**     | Storage, network, screen, cookies, OS    | `useClipboard`, `useNetwork`, `useMediaQuery`, `useLocalStorage`, `useDeviceOS`, `useBatteryStatus`, `useFullscreen` |
| **🖱️ DOM Hooks**       | **12 hooks**     | Listeners, layout dimensions, scroll     | `useClickOutside`, `useLockBodyScroll`, `useKeyPress`, `useSize`, `useWindowSize`, `useMousePosition`                |
| **🔄 Lifecycle Hooks** | **16 hooks**     | Execution control, timers & debouncing   | `useDebounce`, `useInterval`, `useToggle`, `useIsomorphicLayoutEffect`, `useConfirmExit`, `useHold`                  |
| **✨ Animation Hooks** | **5 hooks**      | Dynamic height, motion & state           | `useAutoHeight`, `useInView`, `useControlledState`, `useDataState`, `useMotionValueState`                            |
| **📝 Form Hooks**      | **10 hooks**     | Uploads, limits, reading time, mutations | `useFileUpload`, `useCharacterLimit`, `useReadingTime`, `useSanitizeContent`, `usePagination`                        |
| **🧱 Control-Flow**    | **8 components** | Declarative JSX control-flow             | `<For />`, `<Show />`, `<If />`, `<Switch />`, `<RenderAfter />`, `<Class />`, `<Image />`, `<InView />`             |
| **🛠️ Pure Utilities**  | **11 utils**     | High-performance zero-dep helpers        | `formatBytes`, `rangeMap`, `mapRange`, `sleep`, `bindEvent`, `cookie`, `logger`, `cache`                             |

Install each hook from the registry. Browse the catalog below.
