---
title: useClipboard
description: Copies text to the system clipboard with automatic timeout reset and state feedback.
preview: demo-h-clipboard-01
---

### Example 2: Code Block with Copy Button

<ComponentPreview name="demo-h-clipboard-02" />

### Example 3: Multi-Credential API Keys

<ComponentPreview name="demo-h-clipboard-03" />

## Installation

<ComponentInstallation name="hooks-browser-use-clipboard" />

## Usage

```tsx
import { useClipboard } from '@/hooks/use-clipboard'

const { copy, copied } = useClipboard({ timeout: 2000 })
copy('Text to copy')
```

## API Reference

### Parameters / Props

| Parameter         | Type     | Default | Description                           |
| :---------------- | :------- | :------ | :------------------------------------ |
| `options.timeout` | `number` | `2000`  | Timeout in ms before `copied` resets. |

### Return Value

| Property | Type                                 | Description                       |
| :------- | :----------------------------------- | :-------------------------------- |
| `copy`   | `(text: string) => Promise<boolean>` | Triggers clipboard copy.          |
| `copied` | `boolean`                            | Whether text was recently copied. |
| `reset`  | `() => void`                         | Resets copied state immediately.  |
