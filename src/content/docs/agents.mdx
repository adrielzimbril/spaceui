---
title: AI Agents
description: Build faster and cleaner interfaces with autonomous AI coding agents, specialized skills, and MCP tools.
---

import { IconArrowRight } from '@tabler/icons-react'
import { Frame, FrameHeader } from '@/registry/primitives/frame'
import { Card as CardPrimitives, CardPanel } from '@/registry/primitives/card'
import { Badge } from '@/registry/components/spaceui/badge-squircle'

Space UI is engineered from the ground up for both human developers and **autonomous AI coding agents** (Claude Code, Cursor, Windsurf, GitHub Copilot, Codex, and Antigravity).

Thanks to standard shadcn registry schemas, headless Base UI primitives, and typed Motion animations, AI models can inspect, install, compose, and refactor Space UI components with zero hallucination.

---

## The 3 Pillars of AI Integration

Space UI's agent ecosystem is structured into three dedicated layers:

<Cards className="sm:grid-cols-3">
  <Card
    href="/docs/mcp"
    title="MCP Server"
    description="Direct IDE tool integration exposing component search, TypeScript signatures, and autonomous installation."
    category="Tools · 01"
    badge="MCP Protocol"
    dotColor="bg-blue-400"
    ringColor="bg-blue-300"
    actionLabel="Setup guide"
  />
  <Card
    href="/docs/skills"
    title="Agent Skills"
    description="7 specialized engineering skills in .agents/skills/ covering accessibility, design polish, typography, and layout."
    category="Guides · 02"
    badge="Skill Rules"
    dotColor="bg-green-400"
    ringColor="bg-green-300"
    actionLabel="Explore skills"
  />
  <Card
    href="/docs/llms"
    title="LLM Context"
    description=" Machine-readable endpoints (/llms.txt, /llms-full.txt, and raw per-component markdown)."
    category="Endpoints · 03"
    badge="Context Dumps"
    dotColor="bg-purple-400"
    ringColor="bg-purple-300"
    actionLabel="View endpoints"
  />
</Cards>

---

## How Agents Work with Space UI

When an AI agent operates in your workspace with Space UI:

<div className="not-prose my-6 flex flex-col gap-3">
  {/* Top: AI Agent */}
  <Frame>
    <FrameHeader className="flex flex-row items-center justify-between px-3 py-1.5">
      <span className="text-xs font-semibold text-muted-foreground tracking-wider">AI Agent</span>
      <Badge size="xs" variant="secondary" className="bg-background! text-foreground">
        <span className="relative flex justify-center items-center size-fit">
          <span className="absolute inline-flex h-full w-full rounded-full opacity-75 animate-ping animation-duration-[2.25s] bg-purple-300" />
          <span className="relative inline-flex rounded-full size-2.5 animate-pulse bg-purple-400" />
        </span>
        <span className="relative">Workspace Engine</span>
      </Badge>
    </FrameHeader>
    <CardPrimitives className="rounded-xl overflow-hidden bg-background">
      <CardPanel className="p-4">
        <h4 className="text-sm font-semibold text-foreground">Your AI Assistant</h4>
        <p className="text-xs text-muted-foreground mt-1">
          Claude Code, Cursor, Windsurf, GitHub Copilot, Antigravity
        </p>
      </CardPanel>
    </CardPrimitives>
  </Frame>

  <div className="grid grid-cols-1 gap-3 sm:grid-cols-2">
    <Frame className="flex flex-col h-full">
      <FrameHeader className="flex flex-row items-center justify-between px-3 py-1.5">
        <span className="text-xs font-semibold text-muted-foreground tracking-wider">1. Query &amp; Install</span>
        <Badge size="xs" variant="secondary" className="bg-background! text-foreground">
          <span className="relative flex justify-center items-center size-fit">
            <span className="absolute inline-flex h-full w-full rounded-full opacity-75 animate-ping animation-duration-[2.25s] bg-blue-300" />
            <span className="relative inline-flex rounded-full size-2.5 animate-pulse bg-blue-400" />
          </span>
        <span className="relative">MCP Server</span>
        </Badge>
      </FrameHeader>
      <CardPrimitives className="flex-1 rounded-xl overflow-hidden bg-background">
        <CardPanel className="p-4 flex flex-col justify-between gap-3 h-full">
          <div>
            <h4 className="text-sm font-semibold text-foreground">@spaceui MCP Protocol</h4>
            <p className="text-xs text-muted-foreground mt-1 [&_p]:text-xs [&_p]:sm:text-sm [&_p]:leading-relaxed [&_p]:my-0">
              Autonomous component discovery, typed prop inspection, and CLI installation.
            </p>
          </div>
          <div className="flex flex-wrap gap-1.5">
                       <Badge size="xs" variant="secondary">search_components</Badge>
            <Badge size="xs" variant="secondary">get_component</Badge>
            <Badge size="xs" variant="secondary">add_component</Badge>

          </div>
        </CardPanel>
      </CardPrimitives>
    </Frame>

    <Frame className="flex flex-col h-full">
      <FrameHeader className="flex flex-row items-center justify-between px-3 py-1.5">
        <span className="text-xs font-semibold text-muted-foreground tracking-wider">2. Guide &amp; Refactor</span>
        <Badge size="xs" variant="secondary" className="bg-background! text-foreground">
          <span className="relative flex justify-center items-center size-fit">
            <span className="absolute inline-flex h-full w-full rounded-full opacity-75 animate-ping animation-duration-[2.25s] bg-green-300" />
            <span className="relative inline-flex rounded-full size-2.5 animate-pulse bg-green-400" />
          </span>
        <span className="relative">Agent Skills</span>
        </Badge>
      </FrameHeader>
      <CardPrimitives className="flex-1 rounded-xl overflow-hidden bg-background">
        <CardPanel className="p-4 flex flex-col justify-between gap-3 h-full">
          <div>
            <h4 className="text-sm font-semibold text-foreground">.agents/skills/ Rules</h4>
            <p className="text-xs text-muted-foreground mt-1 [&_p]:text-xs [&_p]:sm:text-sm [&_p]:leading-relaxed [&_p]:my-0">
              Specialized interface engineering constraints and design tokens.
            </p>
          </div>
          <div className="flex flex-wrap gap-1.5">
                       <Badge size="xs" variant="secondary">better-accessibility</Badge>
            <Badge size="xs" variant="secondary">better-ui</Badge>
            <Badge size="xs" variant="secondary">better-layout</Badge>

          </div>
        </CardPanel>
      </CardPrimitives>
    </Frame>

  </div>

  <Frame>
    <FrameHeader className="flex flex-row items-center justify-between px-3 py-1.5">
      <span className="text-xs font-semibold text-muted-foreground tracking-wider">Output</span>
      <Badge size="xs" variant="secondary" className="bg-background! text-foreground">
        <span className="relative flex justify-center items-center size-fit">
          <span className="absolute inline-flex h-full w-full rounded-full opacity-75 animate-ping animation-duration-[2.25s] bg-orange-300" />
          <span className="relative inline-flex rounded-full size-2.5 animate-pulse bg-orange-400" />
        </span>
        <span className="relative">React 19</span>
      </Badge>
    </FrameHeader>
    <CardPrimitives className="rounded-xl overflow-hidden bg-background">
      <CardPanel className="p-4">
        <h4 className="text-sm font-semibold text-foreground">Your React Workspace</h4>
        <p className="text-xs text-muted-foreground mt-1 [&_p]:text-xs [&_p]:sm:text-sm [&_p]:leading-relaxed [&_p]:my-0">
          Base UI primitives, Tailwind CSS v4, and tactile motion in src/components/ui
        </p>
      </CardPanel>
    </CardPrimitives>
  </Frame>
</div>

---

## Best Practices for Agent Prompts

To get the highest quality code from your AI assistant:

### 1. Specify Component Handles

Always reference the exact `@spaceui/<component>` identifier so the agent fetches the official registry item:

```text
Add an interactive notification list using @spaceui/notification-list and @spaceui/badge.
```

### 2. Combine with Specialized Skills

Ask your agent to apply relevant engineering skills during code generation or code review:

```text
Review this dialog component. Apply the better-accessibility skill to verify keyboard traps and aria-describedby bindings.
```

### 3. Use Copy Markdown for Context

If your agent does not have live MCP access, click the **"Copy Markdown"** button at the top of any component page and paste the complete spec into your prompt.
