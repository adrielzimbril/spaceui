---
title: Lens Carousel
description: A high-performance horizontal carousel featuring momentum physics, optical lens sheen, Next.js image optimization, and atmospheric fallbacks.
preview:
  name: block-lens-carousel
  contained: false
---

A dedicated standalone gallery carousel extracted from the immersive lens experience. Features responsive card proportions, horizontal inertial dragging, pointer-capture velocity tracking, atmospheric gradient fallbacks, and polished navigation controls without any monospace badges.

## Installation

<ComponentInstallation name="block-lens-carousel" />

## Usage

```tsx
import BlockLensCarousel, { WORK_LENS_ITEMS } from '@/components/blocks/lens-carousel'
import { LensCarousel } from '@/components/blocks/lens-carousel/lens-carousel'

export default function Showcase() {
  return <BlockLensCarousel />
}
```
